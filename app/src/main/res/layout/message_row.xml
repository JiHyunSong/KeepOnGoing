<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="6dip">

    <TextView
        android:id="@+id/msg_topText"
        android:layout_width="211dp"
        android:layout_height="fill_parent"
        android:layout_alignParentLeft="true"
        android:gravity="center_vertical"
        android:textSize="20px" />

    <TextView
        android:id="@+id/msg_time"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignBaseline="@id/msg_topText"
        android:layout_alignParentRight="true"
        android:gravity="center_vertical"
        android:textSize="20px" />

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/msg_topText"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/msg_person_photo"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:scaleType="fitCenter" />

        <WebView
            android:id="@+id/msg_bottomTextWebView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|center_horizontal"
            android:layout_margin="5dp"
            android:layout_toRightOf="@id/msg_person_photo"
            android:background="@drawable/round_corner2"
            android:clickable="false"
            android:focusable="false"
            android:minHeight="10dp"
            android:padding="5dp"
            android:scrollbars="none" />

        <ImageView
            android:id="@+id/msg_bottomTextImageView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|center_horizontal"
            android:layout_margin="5dp"
            android:layout_toRightOf="@id/msg_person_photo"
            android:background="@drawable/round_corner2"
            android:clickable="false"
            android:focusable="false"
            android:minHeight="10dp"
            android:padding="5dp"
            android:visibility="gone"
            android:scrollbars="none" />


    </RelativeLayout>

</RelativeLayout>